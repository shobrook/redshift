[project]
name = "redshift-cli"
version = "0.3.0"
description = "The world's first vibe debugger"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.0.0",
    "saplings>=6.2.0",
]
